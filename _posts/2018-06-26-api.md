---
layout: post
title: "API de Ciencia de Datos en GECI"
author: Evaristo
tags: programación
---

> **Nota:** Este post describe un flujo de trabajo anterior a las convenciones actuales.
> Consulta la [Guía de Estilo](/guia_de_estilo/STYLEGUIDE) para las convenciones vigentes.

Puedes usar la API RESTful de Ciencia de Datos en GECI mediante [cURL](https://curl.haxx.se/).

## Descargar datos (GET)

Descarga los datos desde el `localhost`:
```bash
curl "http://localhost/v1/data?variable=conteo_nidos&fuente=censo"
```

Descarga los datos de Internet:
```bash
curl -u evaristo.rojas:contraseña "https://islas.org.mx/v1/data?variable=conteo_nidos&fuente=censo"
```

Refina la busqueda agregando parámetros:
```bash
curl "http://localhost/v1/data?\
    variable=conteo_nidos&\
    fuente=censo&\
    taxon=LAAL&\
    region=guadalupe&\
    temporada=2018"
```

Lee los criterios de búsqueda desde un JSON y descárga los datos en formato DPZ (ZIP con _data package_ comprimido):
```bash
curl -X POST http://localhost/v1/data \
  -H "Content-Type: application/json" \
  -d @busqueda.json \
  -o inst/extdata/datapackage.dpz
```

Donde `busqueda.json` contiene:
```json
{
    "variable": "conteo_nidos",
    "fuente": "censo",
    "taxon": "LAAL",
    "region": "guadalupe",
    "temporada": "2018",
    "formato": "dpz",
}
```

## Subir datos (PUT)

Agrega una observación a la base de datos.
```bash
curl -X POST http://localhost/v1/data \
  -F "observacion=@observacion.json"
```

Sube un archivo que contenga varias observaciones.
```bash
curl -X POST http://localhost/v1/data \
  --header "Content-Type: application/octet-stream" \
  --data-binary @data.xlsx
```

```bash
curl -X POST http://localhost/v1/data \
  -F "recurso=@tabla.csv" \
  -F "descriptor=@datapackage.json;type=application/json"
```

```bash
curl -X POST http://localhost/v1/data \
  --header "Content-Type: application/zip" \
  --data-binary @datapackage.dpz
```
